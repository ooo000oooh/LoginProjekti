<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:ec="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
    x:Class="LoginProjekti.MainPage"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="24,10,-12,35">
            <TextBlock Text="{Binding LocalizedResources.Application_name, Source={StaticResource LocalizedStrings}}" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0"/>

        </StackPanel>

        <Grid x:Name="ContentPanel" Width="auto" Height="auto" Margin="14,10,10,-10" Visibility="Visible" Grid.Row="1">
            <RadioButton x:Name="PublicComputerRadioButton" Grid.Row="2"  Content="{Binding LocalizedResources.Public_computer,  Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="119,10,0,0" VerticalAlignment="Top" Width="auto" Height="auto"  Checked="RadioButton_Checked"/>
            <RadioButton x:Name="PrivateComputerRadioButton" Grid.Row="3" Content="{Binding LocalizedResources.Private_computer, Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="119,67,0,0" VerticalAlignment="Top" Width="auto" Height="auto"  Checked="RadioButton_Checked"  />
            <TextBlock x:Name="PrivateComputerInstructionsText" Grid.Row="4" Foreground="Yellow" Visibility="Collapsed" TextWrapping="Wrap" Text="{Binding LocalizedResources.Private_computer_instructions, Source={StaticResource LocalizedStrings}}" Width="auto" Height="auto" Margin="175,136,37,466" />
            <CheckBox x:Name="WebLightCheckbox" Width="auto" Height="auto" Content="{Binding LocalizedResources.Use_outlook_weblight, Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="136,230,0,0" VerticalAlignment="Top" Click="Checbox_Clicked" />
            <TextBlock Name="Use_outlook_weblight_instructions"  HorizontalAlignment="Left" Margin="175,300,0,0" TextWrapping="Wrap" Visibility="Collapsed" Text="{Binding LocalizedResources.Use_outlook_weblight_instructions, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top" Width="244" Height="32"/>
            <TextBox x:Name="userNameTextBox" HorizontalAlignment="Left" Height="72" Margin="142,332,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="277" />
            <TextBox x:Name="passwordTextBox" Grid.Row="7" HorizontalAlignment="Left" Height="72" Margin="136,421,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="262"/>
            <TextBlock HorizontalAlignment="Left" Grid.Row="8" Margin="27,350,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="104" Height="28">
				<Run Text="{Binding LocalizedResources.Username, Source={StaticResource LocalizedStrings}}"/>
				<Run Text=":"/>
            </TextBlock>
            <TextBlock x:Name="PasswordText" Grid.Row="9" HorizontalAlignment="Left" Margin="27,446,0,0" TextWrapping="Wrap" Text="{Binding LocalizedResources.Password, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top"/>
            <Button x:Name="LoginButton" Grid.Row="10" Content="{Binding LocalizedResources.Login, Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="200,553,0,0" VerticalAlignment="Top" Width="213" Click="Button_Click"/>
            <TextBlock x:Name="LoginFailedText" Grid.Row="11" Foreground="Yellow" Visibility="Collapsed" HorizontalAlignment="Left" Margin="151,498,0,0" TextWrapping="Wrap" Text="{Binding LocalizedResources.LoginFailed, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top" Width="275" Height="31" RenderTransformOrigin="0.567,1.009"/>
            


        </Grid>

        <Grid x:Name="LoggedInPanel" Grid.Row="1" Margin="24,10,0,-10" Visibility="Collapsed">
            <TextBlock x:Name="WelcomeText" Foreground="Green"   HorizontalAlignment="Left" Margin="138,414,0,0" TextWrapping="Wrap" Text="{Binding LocalizedResources.WelcomeText, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top" Width="275" Height="90"/>
            <Button x:Name="BackButton" Content="{Binding LocalizedResources.Back, Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="183,469,0,0" VerticalAlignment="Top" Width="213" Click="BackButton_Clicked"/>
        </Grid>

    </Grid>

</phone:PhoneApplicationPage>